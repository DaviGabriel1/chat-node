<div class="home-header">
    {{#if search}}
        <h1>Você está procurando por: <span>{{search}}</span></h1>
        {{#if toughtsQty}}
            <p>foram encontrados <span>{{toughtsQty}}</span> Publicacoes</p>
        {{else}}
            <p>nenhuma publicação foi encontrado para o termo <span>{{search}}</span></p>
        {{/if}}
    {{else}}
        <h1>todos os pensamentos</h1>
    {{/if}}
    <form action="/" method="GET">
        <input type="text" name="search" placeholder="Está procurando por algo?">
        <input type="submit" class="btn" value="buscar">
    </form>
    <div class="order-container">
        <span>Ordenar por:</span>
        <form action="/" method="GET">
            <input type="hidden" name="search" value="{{this.search}}">
            <input type="hidden" name="order" value="new"> 
            <button type="submit"><i class="bi bi-arrow-up"></i></button>
        </form>
        <form action="/" method="GET">
            <input type="hidden" name="search" value="{{this.search}}">
            <input type="hidden" name="order" value="old"> 
            <button type="submit"><i class="bi bi-arrow-down"></i></button>
        </form>
        <a href="/">limpar</a>
     </div>
     
</div>
<div class="toughts-container">
        {{#each toughts}}
            <figure>
                <blockquote>
                    "{{this.title}}"
                </blockquote>
                <figcaption>Por <span>{{this.User.name}}</span></figcaption>
            </figure>
        {{/each}}
</div>